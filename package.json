{
  "name": "koa-ctx-cache-control",
  "version": "1.0.0",
  "description": "Augments Koa with ctx.cacheControl(maxAge)",
  "main": "lib",
  "scripts": {
    "lint": "standard lib/*.js test/*.js",
    "test": "mocha test",
    "test-cov": "nyc --cache node_modules/mocha/bin/_mocha test && nyc report --reporter=lcov"
  },
  "author": "Jonathan Ong <me@jongleberry.com> (http://jongleberry.com)",
  "license": "MIT",
  "repository": "koajs/ctx-cache-control",
  "dependencies": {
    "ms": "^0.7.1"
  },
  "devDependencies": {
    "koa": "^2.0.0",
    "mocha": "^3.1.0",
    "nyc": "^6.4.4",
    "standard": "^7.1.0",
    "supertest": "^1.2.0"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "koa",
    "cache",
    "control",
    "context",
    "response"
  ]
}
